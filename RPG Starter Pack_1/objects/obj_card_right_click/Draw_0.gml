if (description != undefined) {
    draw_set_font(Font_small);
    draw_text_ext(20, 15, description, 20, 300);
    //alarm[0] = 180;
    // If card gets right clicked display the text on top of screen
    // If another/different card gets right clicked destroy old text and add new
}
